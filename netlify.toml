[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# SPA fallback (static files & functions are served before this)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
